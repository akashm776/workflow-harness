[project]
name = "workflow-harness"
version = "0.1.0"
description = "Deterministic policy-governed dynamic workflow harness"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
